我把目录下除了demo文件夹以外的内容清除了，然后换成了最新的，从GITHUB上下载下来的代码。本地代码作为参考，然后我需要你在github上，新增以下功能。
1. 新增一次性录制功能，即在添加录制链接时可选本次录制是否为一次性。同时已有链接也可以被设置为一次性录制。
*一次性录制含义：本次正常录制（如是存量则为下一次正常录制），但是在停止直播后某一段时间内（可设置阈值，例如3个小时），没有再次开播即被标记为“待删除”状态，然后在某个时间后（可设置，例如7天）删除它（只删链接，不删文件）
*设置中可设置是否默认链接都为一次性录制，即添加链接时，一次性录制选项默认勾选
2. 在添加链接时，可识别抖音的分享链接，从中正确提取链接。
例如：2- #在抖音，记录美好生活#【QwQ】正在直播，来和我一起支持Ta吧。复制下方链接，打开【抖音】，直接观看直播！ https://v.douyin.com/nuxAedcdFF8/ 1@3.com :2pm
它的正确链接应为：https://v.douyin.com/nuxAedcdFF8/
3. 当前前端界面，直播间列表有以下显示菜单（主播名称/运行状态/操作），我需要在主播名称和运行状态间新增三项，即添加链接时间/最近一次直播时间/文件夹大小
添加链接时间：何时添加链接（有一点需要注意，由于这是新功能，可能现存的链接没有这项信息，那就找到该链接创建的录制文件夹，使用创建文件夹时间）
最近一次直播时间：最近一次被监控到直播的时间
文件夹大小：该链接所创建的文件夹大小（值得注意的是，现行逻辑是，文件夹是以直播间名字创建（我记得应该是），所以在直播间改名后或者主播改名后会新建一个文件夹，所以要注意汇总它实际创建的所有文件夹大小。这一点在‘添加链接时间’且为现存链接时也需要注意。）
4. 直播文件小文件合并功能。 -- 当前逻辑时，每次接收到的直播片段，在停止后即结束，然后开始后续动作（比如转码为MP4格式）。现在我需要增加一个功能，在某个片段结束的一段时间内（可设置），如果再次收到直播片段即等待这段片段接收完成后再统一合并并转码。
5. 当前项目在录制链接较多时，启动非常缓慢，希望你能改进这一点（哪怕是前后端先起来，请求数据慢慢发也行，避免启动后很长一段时间都打不开前端）
6. 并‘直播间列表’的‘操作’下，在配置之后新增字段，用于配置录制时间段，只在某个时间内才录制，其他时间段只监控，不录制，并且清晰的显示已配置的录制时间段（同时‘运行状态’那里也要同步显示，可以新增一个状态）。然后设置录制时间段需要足够灵活，例如可配置模板等等。
7. 直播间列表可多选，勾选后可以多项删除/停止录制/启动录制/配置录制时间段/更改为一次性录制/更改为持久性录制等等
8. 增加定时重启功能。重启也分为硬重启和软重启。
*硬重启，彻底重启整个应用。
*软重启，切断录制请求，不发送流量到服务器，然后在一段时间后（可设置）重新请求数据。这个功能也可用于解决新增需求5

必须遵守一下纪律：
1. 不得上传任何本机文件
2. 只能在github中在线修改
3. 不得破坏本机环境（即系统环境）
4. 在你干活前，如有其他问题需要确认，或者不理解以上信息，请先向我提出，得到确切答复后再继续。

=====================================================================
【确认问答记录】
=====================================================================

操作方式：
- 只能通过浏览器在 GitHub Web 编辑器逐个文件修改
- 仓库地址：https://github.com/lingyue103/bililive-go （fork 自 bililive-go/bililive-go）
- 可由用户协助登录 GitHub

需求1（一次性录制）确认：
- "待删除"状态需要在前端显示为新的运行状态标签，"一次性录制中"也需要显示
- 两个阈值（未开播超时标记待删除、待删除后多久删除链接）默认为全局配置项
- 也可在单个链接的配置中单独覆盖这两个阈值

需求3（前端列表新增三列）确认：
- 文件夹归属识别：可在文件夹中留下 URL 或其他唯一标识用于确认归属，但需注意效率问题，不能遍历所有文件夹
- 对于现存的、没有标识的文件夹，直接按名称匹配即可
- 需要自行设计高效方案

需求4（小文件合并）确认：
- 场景确认：一次录制结束后，在设定时间内如果同一直播间再次开播并录制，等这次也结束后合并为一个再转码
- 如果再次开播时直播间名称变了，则不适用于合并规则，按正常处理
- 合并后是否删除原始片段文件：现行项目中已有解决方案，沿用即可

需求6（录制时间段配置）确认：
- 模板定义：可预设时间段组合（如"工作日晚间 18:00-23:00"）保存为模板，多个直播间复用
- 时间段不支持跨天（如 22:00-次日 02:00 不支持），只能按具体时间
- 如果直播从录制时段一直持续到了预设的停止录制时间，不要掐断，等待它自然结束
- 录制时间段可指定星期几（如周一、周末等）

需求8（定时重启）确认：
- Docker 内只支持软重启，不支持硬重启
- 非 Docker 环境支持硬重启（依赖 launcher 实现进程重启）
- 软重启的时间段为全局配置，例如配置每天凌晨3点执行软重启，3点10分恢复
- 软重启含义：切断录制请求，不发送流量到服务器，等待设定时间后重新请求数据
